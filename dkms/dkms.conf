PACKAGE_NAME="thunderbolt-tbfix"
PACKAGE_VERSION="2.10"

BUILT_MODULE_NAME[0]="thunderbolt"
BUILT_MODULE_LOCATION[0]="drivers/thunderbolt"
DEST_MODULE_LOCATION[0]="/updates"

BUILT_MODULE_NAME[1]="thunderbolt_net"
BUILT_MODULE_LOCATION[1]="drivers/net/thunderbolt"
DEST_MODULE_LOCATION[1]="/updates"

BUILT_MODULE_NAME[2]="rdma_rxe"
BUILT_MODULE_LOCATION[2]="drivers/infiniband/sw/rxe"
DEST_MODULE_LOCATION[2]="/updates/dkms"

MAKE[0]="make KDIR=${kernel_source_dir}"
CLEAN="make KDIR=${kernel_source_dir} clean"

# Stash the distro thunderbolt_net.ko out of the module tree for this kernel so
# only our DKMS build is resolvable (no early-boot -22 symbol-mismatch flood
# from the upstream-CRC distro module loading against our patched core). Runs
# per-kernel, including new kernels dkms autoinstalls for. Reversed on package
# removal by the debian prerm (tbfix-net-suppress.sh restore).
POST_INSTALL="tbfix-net-suppress.sh disable"

AUTOINSTALL="yes"
